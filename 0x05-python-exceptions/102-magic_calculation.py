#!/usr/bin/python3

def magic_calculation(a, b):
    res = 0
    for item in range(1, 3):
        try:
            if a <= item:
                res += a ** b / i
            else
            raise Exception('Too far')
        except Exception:
            return (a + b)
    return (res)
